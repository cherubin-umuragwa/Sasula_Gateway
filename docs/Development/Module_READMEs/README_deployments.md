# Module README — `deployments/`

> Summary: Network deployment address maps.

## Files
- `baseSepolia.json` — addresses for current testnet deployment.

## Usage
- Frontend loads addresses from `.env`, but this file provides a canonical reference.
